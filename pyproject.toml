[tool.uv.workspace]
members = ["packages/*"]

[tool.uv]
override-dependencies = [
    # 결정 A10: GUI용 opencv 차단 — scenedetect의 하드 의존을 headless로 단일화
    "opencv-python; sys_platform == 'never'",
    # 결정 A10: torch 배제 — mlx-whisper가 의존 선언하지만 전사 런타임에서 미사용
    "torch; sys_platform == 'never'",
]
