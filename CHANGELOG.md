# 변경 이력

[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식.

이 저장소는 패키지 두 개를 담고 **버전은 서로 독립**이다. 아래도 패키지별로 나뉜다.

| 패키지 | 태그 |
|---|---|
`analysis-video` (코어 CLI) | `core-v<버전>` |
`analysis-video-gui` (디버깅 GUI) | `gui-v<버전>` |

---

# analysis-video (코어 CLI)

## [0.1.1] — 2026-08-14

0.1.0을 PyPI에 올린 뒤 **배포판 그대로** 강의 영상 7편을 돌려 나온 세 가지를 고쳤다
(자막 사다리의 모든 단을 한 번씩 밟는 구성 — 사이드카·내장 트랙·다국어 선택·강제 자막·
whisper 두 언어). 산출물 스키마는 그대로라 0.1.0으로 만든 분석 디렉터리를 계속 쓸 수 있다.

### 변경

- **`--transcript`로 지목한 자막은 품질 검사에 걸려도 쓴다.** 큐 수·커버리지·롤업·구간
  네 검사는 도구가 **스스로 찾아낸 후보 여럿에서 고르기 위한** 것인데, 지목한 파일에까지
  적용돼 사용자의 판단을 뒤집고 있었다. 실제로 커버리지 11%짜리 강제 자막을 `--transcript`로
  지정하면 종료코드 2 · `transcript-rejected`로 거부됐고, `hint`가 안내한 두 대안(자동 탐색에
  맡기기 · `--no-subtitles`)은 **둘 다 그 파일을 쓰지 않아서** 알고도 쓰겠다는 통로가 아예
  없었다. 이제 판정을 거부에서 **신고**로 낮춘다: 그대로 채택하고, 사유는
  `transcript.json`의 `source.notes`에 `지정한 자막이라 그대로 씁니다 — …` 형태로 남기며
  stderr에 경고를 찍는다. 자동 탐색(사이드카·내장 트랙)에서는 네 검사가 그대로 거부로 남는다.
  여전히 멈추는 것은 **쓸 것이 없는 파일**뿐이다 — 없는 파일(`transcript-not-found`),
  읽지 못하는 파일, 큐가 하나도 없는 파일(`transcript-rejected`).

- **`<video>` 자리에 `.analysis` 디렉터리를 줄 수 있다** — `analyze`·`split`·`transcribe`·
  `frames`·`frame`·`review`·`clean`·`status`·`debug-report` 아홉 개 전부. 분석이 끝난 뒤의
  작업에서 사용자가 손에 든 경로는 산출물 디렉터리인데, 그것을 주면 뒤에 `.analysis`를 한 번 더
  붙여(`lecture.mkv.analysis.analysis`) **멀쩡히 끝난 분석을 "아직 안 됨"이라고 답했다.**
  GUI는 처음부터 두 형태를 다 받았으므로, GUI에서 보던 경로를 CLI에 붙여 넣으면 두 도구의
  답이 갈렸다. 경로를 푸는 규약을 코어 한 곳(`manifest.resolve_target`)으로 모으고 CLI와
  GUI가 함께 쓰게 했다. `state.json`이 없는 디렉터리는 `not-analyzed`, 기록된 원본이 사라진
  디렉터리는 `source-missing`, 디렉터리와 `--out`을 함께 준 것은 `target-conflict`
  (모두 종료코드 2).

### 수정

- **macOS에서 매 실행 stderr에 찍히던 ObjC 중복 클래스 경고 두 줄을 걷어냈다.**
  `av`와 `opencv-python-headless`(scenedetect가 끌어온다)가 각자 FFmpeg를 동봉해
  `libavdevice`의 AVFoundation 캡처 클래스를 두 번 등록하는 상류 패키징 충돌이다. 사본 어느
  쪽도 우리가 뺄 수 없고 그 기능을 쓰지도 않아 실제 영향은 없지만, 문구가 "mysterious
  crashes"라 정상 실행이 고장처럼 보였다. cv2를 적재하는 구간의 파일 서술자 2를 받아 두었다가
  **그 한 줄만** 걸러 내고 나머지는 그대로 돌려준다 — 통째로 묻으면 같은 적재 과정에서 나는
  진짜 오류(다른 dylib 실패)까지 사라진다. macOS가 아니면 아무 일도 하지 않는다.

## [0.1.0] — 2026-08-14

첫 릴리스다. 아래는 "직전 버전과의 차이"가 아니라 **0.1.0 이 무엇인가**의 목록이고,
절 이름(파괴적 변경·추가·변경·제거·수정)은 개발 중에 무엇이 뒤집혔는지를 남긴 것이다 —
0.1.0 부터 쓰는 사람에게는 전부 "처음부터 그런 것"이다.

### 파괴적 변경

- **산출물 스키마가 올랐다 — `state.json` `@1` → `@4`, `metadata.json` `@1` → `@3`.
  옛 분석 디렉터리는 재사용할 수 없다 — 마이그레이션은 없다.**
  `state.json`·`metadata.json`에 적히는 형식 버전을 이제 **읽을 때마다 대조하고**, 맞지 않으면
  그 자리에서 거부한다: 종료코드 2 · `schema-mismatch` · `hint`는 "`--out`으로 새 디렉터리를
  지정하거나, 이 디렉터리를 지우고 처음부터 다시 분석하세요"(`details`에 `path`·`expected`·
  `found`). 버전 필드를 쓰기만 하고 읽지 않으면 아무 일도 하지 않는다 — 이번에 잠근 것은
  "현재 버전을 쓴다"가 아니라 **"현재 버전이 아닌 것을 읽지 않는다"**이다. 검사는 읽는 함수
  한 곳에 있어 `status`·`transcribe`·`frames`·`frame` 어느 입구로 들어와도 같은 자리에서 막힌다.

  왜 거부인가(`@1` → `@2`): 자막을 전사 출처로 채택하면서 `state.json`에 칸이 늘었으므로
  (아래 "추가" 참조) 옛 디렉터리를 그대로 들이면 그 칸을 짚는 자리에서 `KeyError`로 죽는다.
  그것은 종료코드 1(`internal`)이라 호출 에이전트에게는 **도구의 버그**로 보이고, 버그로
  보이면 같은 명령을 재시도해 같은 자리에서 또 죽는다. 형식 불일치는 입력의 문제이므로
  입구에서 거부하고 다음에 할 일을 함께 알려준다.

  **`state.json`이 한 번 더 올라 `@2` → `@3`이 됐다 — `@2`로 만든 디렉터리도 같은 방식으로
  거부된다.** 자막 언어를 다루면서 `stages.transcribe.outputs`에 언어 세 칸(`language` ·
  `target_language` · `language_mismatch`)이 생긴 것이 직접 원인이다. `--sub-lang`을 주고
  완료된 전사를 다시 여는 실행은 그중 `language`를 읽어 요청 언어와 대조하므로(재사용 경로),
  칸이 없는 `@2` 디렉터리는 거기서 `KeyError` = 종료코드 1로 죽는다 — `@1`을 막은 것과 같은
  이유로 입구에서 종료코드 2로 거부하는 것이 맞다. 플래그를 주지 않은 실행은 그 칸을 읽지 않아
  살아남지만, 그때는 결과 JSON에서 언어 세 칸만 빠진 채 나간다: **출력 계약이 디렉터리의 나이에
  따라 달라지는 것**이라 어차피 거부가 맞다.

  **`state.json`이 또 한 번 올라 `@3` → `@4`가 됐다.** `stages.split.outputs`의
  `audio`(뽑아 둔 wav의 경로 또는 `null`)가 **`has_audio`(불리언)로 바뀌었기** 때문이다
  (아래 "제거" 참조). 칸 이름을 그대로 두고 타입만 바꾸면 `if outputs["audio"]:`로 읽던
  코드가 조용히 통과해 **깨져야 할 자리가 안 깨진다** — 그래서 이름까지 바꿨고, 이름이
  바뀐 이상 옛 디렉터리는 그 칸을 짚는 자리에서 `KeyError`(종료코드 1)로 죽는다. `@1`·`@2`를
  막은 것과 같은 이유로 입구에서 종료코드 2로 거부한다.

  **`metadata.json`도 `@2` → `@3`으로 올랐다.** 여기서는 칸이 하나 **늘었다** — 읽기용
  축소 사본의 장수·토큰·크기를 적는 `images` 블록이다(아래 "추가" 참조). 이 칸은 선택이
  아니다: `context.render`가 **폴백 없이** 읽어 `context.md`의 이미지 경로와 비용 문단을
  만들기 때문에, 칸이 없는 옛 `metadata.json`은 그 자리에서 `KeyError`가 된다.
  (직전까지 `metadata.json`은 `@1` → `@2`에서 멈춰 있었다 — 언어 세 칸이 `state.json`에만
  생겨 모양이 그대로였기 때문이고, 둘을 습관적으로 함께 올리면 "이 디렉터리의 어느 파일이
  낡았는가"가 흐려진다는 판단이었다. 이번에는 실제로 `metadata.json`의 모양이 바뀌었다.)
  거부 판정에는 차이가 없다 — 어느 쪽이든 옛 형식이면 같은 `schema-mismatch`로 막힌다.

  **하위 호환 코드를 두지 않기로 한 결정에 따른 의도된 결과다.** 이 버전 이전에 만든 로컬 분석
  디렉터리는 전부 재분석 대상이다. 지울 것은 분석 디렉터리뿐이고 영상·자막 원본은 건드리지 않으며,
  지우기 전에 옛 `context.md` 를 읽어 두는 것도 그대로 된다(도구가 대신 지우지 않는다).
- **[내부 API] `split.split_media()` 반환의 첫 칸이 `audio: Path | None` 에서
  `has_audio: bool` 로 바뀌었다.** 경로였을 때 그 칸은 **사실과 위치를 한 칸에 겹쳐**
  담고 있었고, 소비자가 실제로 묻는 것은 사실 쪽 하나뿐이다("오디오가 있는가 →
  위스퍼, 없는가 → 빈 전사"). 이름을 함께 바꾼 것은 의도다 — 칸 이름을 두고 타입만
  불리언으로 바꾸면 `res.audio` · `outputs["audio"]` 로 읽던 코드가 조용히 통과한다.
  이제 그 접근은 `AttributeError` · `KeyError` 로 즉시 멈춘다. 같은 이름 변경이
  `state.json` 스키마를 `@4` 로 올린 직접 원인이다.
- **[동작 변경] 위스퍼 기본 모델이 `tiny` → `small` 이다.** `--model` 없이 돌리면
  받는 가중치가 약 74MB → 약 460MB가 되고, 추론도 그만큼 느려진다. 자막을 먼저 쓰게 된
  뒤로 위스퍼가 도는 경우는 "자막이 아예 없는 영상"만 남는다 — 대사를 얻을 다른 수단이
  없어 전사 품질이 곧 산출물 전체의 품질이 되는 자리라, 가장 아쉬운 곳에 가장 약한 모델을
  두지 않기로 했다. `tiny` 는 정반대 가정(자막 유무와 무관하게 위스퍼가 늘 돌던 시절,
  속도로 파이프라인 전체를 지키던 값)에서 고른 값이다. **이전과 같이 하려면 `--model tiny`.**
  정직한 한계: 이 값은 실측으로 확정한 것이 아니다. 이 저장소의 STT 실측은
  "Apple Silicon·mlx·`tiny` 에서 37.6배 실시간" 하나뿐이고 `small` 의 속도·정확도는 재지 않았다.
- **[동작 변경] 전사를 다시 쓰면 `frames` 가 미완료로 돌아간다.** `transcribe --force` 든,
  자막이 바뀌어 일어난 자동 재전사든 마찬가지다. `context.md`·`metadata.json`은 **이전** 대사를
  화면에 배분해 둔 상태이고 그 대사가 어느 전사에서 왔는지는 어디에도 적혀 있지 않아, 그대로
  두면 아무도 낡은 줄 모른 채 옛 대사를 읽는다. 지우는 것은 완료 표시뿐이고 파일은 남는다
  (다시 도는 `frames` 가 `runs.reset_unit`으로 자기 산출물을 결정적으로 갈아엎으므로, 여기서
  파일까지 지우면 삭제 책임이 두 곳으로 갈린다). **재전사 뒤에는 `frames` 또는 `analyze` 를 한
  번 더 돌려야 한다** — 그전까지 `frame --at` 은 종료코드 3(`stage-order`)으로 멈추고 실행할
  명령을 알려준다. 재사용·건너뜀은 이 무효화를 일으키지 않는다.

### 추가
- **자막을 전사 출처로 채택한다. 위스퍼는 마지막 폴백이 되었다.** 전사는 이제
  ①`--transcript` 로 지정한 파일 ②이 영상이 이미 가진 자막(영상 옆의 사이드카와 컨테이너
  내장 트랙을 **한 줄로** 세워 언어부터 비교한다 — `--sub-lang` 항목) ③위스퍼 순으로 대사를
  얻는다. 위스퍼 전사는 오디오에서 **추론한** 텍스트지만 자막은
  사람이 적은 원문이라, 고유명사·전문용어·숫자처럼 추론이 흔들리는 자리에서 흔들리지 않고
  시각도 제작 단계에서 이미 화면에 맞춰져 있다. **자막을 쓰면 위스퍼 가중치를 받지 않아
  전사 전체가 오프라인이 된다.** SRT·VTT·SMI를 읽는다(ASS/SSA는 미지원).
- **쓰면 안 되는 자막은 내용으로 거부한다.** 큐 5개 미만 · 커버리지 30% 미만(강제 자막) ·
  큐가 영상 길이 밖(다른 영상의 자막) · 롤업 비율 30% 초과(자동 생성 자막). 특히
  자동 생성 자막은 파일명으로 구분할 수 없다 — yt-dlp가 `--write-subs` 와
  `--write-auto-subs` 를 같은 이름(`영상.ko.vtt`)으로 쓰기 때문에 내용을 봐야 한다.
  ASR을 다시 쓸 바에는 모델을 고를 수 있고 어느 엔진이 돌았는지 기록에 남는 위스퍼가 낫다.
- `--transcript PATH` (`analyze` · `transcribe`) — 이 자막 파일을 전사로 쓴다.
  **지목한 파일이 거부되면 다른 출처로 폴백하지 않고 종료코드 2로 멈춘다.** 사용자가
  요청한 것과 다른 산출물이 같은 이름으로 나오면 안 된다.
- `--sub-lang CODE` (`analyze` · `transcribe`) — **쓸 자막의 언어**를 고른다. 사이드카 파일과
  내장 트랙을 **한 줄로 세운 뒤** 그 순위의 **1차 키**가 이 값이다: 일치하는 것 > 언어 태그
  없는 것 > 그 외. **출처는 언어가 같은 후보들 사이의 동점 처리일 뿐이라**(사이드카 > 내장
  트랙), 요청한 언어의 내장 트랙이 다른 언어의 사이드카를 이긴다. **불일치는 탈락이 아니라
  순위 하락이다** — 다른 언어의 자막이라도 자막이 없는 것보다 낫기 때문에, 일치하는 것이
  하나도 없으면 남은 최선을 그대로 쓴다. `ko` 와 `kor` 은 같은 언어로
  본다(ISO 639-1 ↔ 639-2, 지역 부표 무시).
  **주지 않아도 시스템 로케일이 목표 언어가 된다**(`LC_ALL` > `LC_MESSAGES` > `LANG` >
  `LANGUAGE`, 1차 부표만) — 한국어 환경이면 플래그 없이도 `ko` 자막이 골라지고, 뒤집으면
  같은 명령이 머신에 따라 다른 자막을 고를 수 있다(그래서 로케일이 정했다는 사실도 기록에
  남는다). `--language` 와 역할이 다르다 — 그쪽은 위스퍼가 오디오를 들을 때의 **음성 언어
  힌트**이고 자막 선택에는 쓰이지 않는다.
- **번역은 하지 않는다(설계 결정).** 채택된 자막도 위스퍼 결과도 목표 언어와 다를 수 있고,
  그때 도구가 하는 일은 그 사실을 정확히 신고하는 데까지다 — `transcript.json` 의
  `source.language`(실제 전사 언어)·`source.target_language`(요청 언어), 결과 JSON의
  `language_mismatch`(둘이 다른가), 그리고 stderr 경고 한 줄이 전부이고 대사는 원문 그대로 나간다. 대사는 화면·시각과 대조하는 재료라 옮겨 적으면 프레임 속 문구와 어긋나고,
  위스퍼의 번역 기능은 목적지가 영어로 고정이라 "한국어로 달라"를 애초에 만족시키지 못한다.
- `--no-subtitles` (`analyze` · `transcribe`) — 자막을 아예 보지 않고 음성을 전사한다.
- `split` 이 컨테이너 내장 자막 트랙을 **전부 열거하고** 쓸 수 있는 것을
  `<분석디렉터리>/subs/track{스트림인덱스}.srt` 로 뽑는다. 코덱이 무엇이든(subrip·mov_text·
  ass) SRT 하나로 재작성하므로 읽는 쪽이 포맷별 파서를 갖출 필요가 없다. 비트맵 자막
  (PGS·DVD·DVB)과 강제 자막은 사유를 달아 열거만 하고 뽑지 않는다 — 못 쓴 트랙을 목록에서
  지우면 "이 영상에는 강제 자막밖에 없었다"가 어디에도 남지 않아 위스퍼가 돈 이유를
  사후에 설명할 수 없다.
- `transcript.json` 에 `source` 필드. 채택된 출처(`kind`·`path`·`track`·`format`·`language`·
  `target_language`), 큐 수·커버리지·구간, 그리고 **거부된 후보와 그 사유**(`notes`)가
  한국어로 남는다. `target_language` 는 요청된 언어(`--sub-lang` 또는 로케일)이며 모든 출처에서
  항상 존재한다.
  위스퍼·빈 전사 경로도 같은 모양으로 채운다. 기존 키(`text`·`segments`·`backend`·`model`)의
  의미는 바뀌지 않았다.
- 종료코드 2의 새 사유 3종 — `transcript-not-found`(지정한 자막 파일 없음) ·
  `transcript-rejected`(지정한 자막이 검사 불합격) · `conflicting-options`
  (`--transcript` 와 `--no-subtitles` 동시 지정).
- `state.json` 새 칸 — `source.subtitle`(전사 입력 자막의 지문) ·
  `stages.split.outputs.subtitles`(내장 트랙 목록) ·
  `stages.transcribe.outputs.{source_kind, source_path, language, target_language,
  language_mismatch}`. 여기까지는 전부 키 추가이고, 언어 세 칸이 state 스키마를 `@3`으로
  올린 직접 원인이다. 하나는 **교체**다 — `stages.split.outputs.audio`(경로 또는 `null`)가
  `has_audio`(불리언)가 됐고(아래 "제거"), 그것이 `@4`로 올린 원인이다.
- **`review` — 호출 AI가 쓴 분석문이 사는 자리.** 지금까지 파이프라인은 `context.md`에서
  끝났는데, 사용자가 AI에게 시킨 것은 **분석**이고 context.md는 그 재료다. 분석 결과는
  채팅에만 남아 세션과 함께 사라졌고, `status`는 "전부 완료"라면서 정작 시킨 일이 어디에도
  없는 상태를 만들었다. `analysis-video review <video> [--run 이름] --write -` 가 본문을
  **표준입력**으로 받아 `<분석디렉터리>/reviews/<단위>.md` 에 둔다. `--write` 없이 부르면
  상태만 조회한다.
  - 정본이 **분석 단위 디렉터리 밖**인 것이 핵심이다 — 안에 두면 `runs.reset_unit`의
    `shutil.rmtree(unit)`이 임계를 한 번 조정할 때마다 AI가 쓴 글을 지운다.
  - 코어가 소유하는 것은 머리말(HTML 주석 속 JSON + 마크다운 표)뿐이고 지문은
    **`runs/<run>/context.md`의 sha256 하나**다. `state.json`에는 **기록하지 않는다** —
    `mark_done`이 스테이지 칸을 통째로 덮고 `invalidate_stage`가 그것을 `pop`하므로
    거기 두면 `frames`가 한 번 돌 때마다 장부가 전멸한다. 파일 머리말에 두면 쓰기가
    `os.replace` 한 번으로 끝나 파일과 장부가 갈릴 자리 자체가 없다.
  - 상태 넷: `missing` · `unreadable` · `stale`(읽은 context.md가 그 뒤 바뀜) · `current`.
    동작 넷: `created` · `unchanged` · `refreshed`(본문은 같고 지문만 갱신) · `updated`.
    유효한 리뷰를 **다른 본문으로** 덮으려 하면 쓰지 않고 `review-exists`(종료코드 2)로
    멈춘다 — `--force` 가 있어야 덮는다.
  - `--export-dir DIR` — 정본을 쓴 **뒤** 그 폴더로 사본을 하나 더
    (`<영상 파일명>.<단위>.review.md`). 정본 위치는 바뀌지 않고, 사본은 어디에도 기록하지
    않는다 — 기록하면 사용자가 지우거나 옮긴 뒤 끊어진 참조가 남는다.
  - 종료코드 2의 새 사유 — `stdin-is-tty` · `empty-review` · `review-not-text` ·
    `review-too-large` · `review-contains-marker` · `review-exists` ·
    `export-not-a-directory`. 종료코드 3의 새 사유 — `context-missing`.
  - **폐기한 points.json(샌드위치)의 재래가 아니다.** 그쪽은 AI 산출물이 *하류 결정적
    스테이지의 입력*이 되어 시각 검출과 같은 출력 자리를 놓고 경쟁했다. 이쪽은 **말단**이라
    아무것도 review.md를 소비하지 않는다.
- **`clean` — 되만들 수 있는 산출물 정리(새 모듈 `clean.py`).**
  `analysis-video clean <video> [--level cache|images]`. **`--level` 없이 부르면 지우지
  않고 보고만 한다** — 무엇이 얼마나 있고, 레벨마다 얼마를 회수하며 그 대가가 무엇인지.
  레벨은 **누적**이다: `cache` = `video.mkv`(대가 없음 — 없으면 원본 영상에서 그대로 읽고
  프레임이 바이트 동일함을 실측 확인) / `images` = 거기에 `runs/*/frames/`(GUI와 정밀
  확인이 안 되지만 `context.md`는 `read/`를 가리키므로 그대로 읽힌다).
  **어느 레벨에서도 지우지 않는 것**: `reviews/`(되만들 수 없다) · `transcript.json` ·
  `runs/*/read/` · `runs/*/requested/` · `metadata.json`·`context.md`·`frames.json` ·
  `detect_signals.npz`·`detect_adaptive.json`. 실측 9.8MB → 1.4MB.
  기본값을 삭제가 아니라 보고로 둔 이유는 `review`다 — 그전까지 이 디렉터리의 모든 것은
  원본 영상만 있으면 되만들 수 있었지만 분석문은 아니고, 공간을 비우려는 `rm -rf` 한 번에
  그것이 함께 사라지면 안 된다.
- **읽기용 축소 사본(새 모듈 `budget.py`)과 비용 표기.** `runs/<단위>/read/` 에 긴 변을
  줄인 사본을 두고 **`context.md`의 `![](…)`가 그쪽을 가리킨다**(전에는 `frames/`).
  원본 해상도는 `runs/<단위>/frames/` 에 같은 파일명으로 그대로 남는다. 이유는 디스크가
  아니라 **컨텍스트 창**이다: 원본 해상도(1920x1080)는 장당 1,843토큰이라 1시간 강의
  여섯 편이 전부 100만 토큰을 넘겼는데(1.08M~3.95M), 사본은 장당 442토큰 — **4.17분의 1**
  이라 0.26~0.95M로 내려온다. 본문 가독성은 판서·수식·슬라이드 세 종류에서 실측 확인했다 —
  잃는 것은 OS 크롬·썸네일 같은 주변부다.
  - `context.md` 머리에 **비용 문단**이 생겼다(사본 크기 · 장수 · 전부 열었을 때의 토큰 ·
    산정 규칙). 장수만 적고 비용을 안 적으면 "필요한 것만 열어라"는 안내가 근거를 주지 못한다.
  - `metadata.json`에 `images` 블록 — `{"read_dir", "long_edge", "count", "tokens",
    "rule"}`. `context.render`가 **폴백 없이** 읽는다(metadata 스키마 `@3`의 직접 원인).
    규칙을 값과 함께 내보내는 이유는 그 공식이 Anthropic 계열 모델의 것이기 때문이다 —
    다른 모델을 쓰는 소비자가 자기 것이 아님을 알 수 있어야 한다.
  - 새 플래그 **`--read-long-edge 픽셀`**(`analyze`·`frames`) — 사본의 긴 변
    (기본값은 상수 `budget.READ_LONG_EDGE`, 값은 `agent-guide` 가 인쇄한다). 임계 셋과
    성질이 달라(판정선이 아니라 크기, 그리고 정수) 별도 인자로 뒀다. `build_frames` 에도
    같은 이름의 인자가 생겼고, 반환에 `"images"` 키·`params` 에 `read_long_edge` 가 실린다.
  - `media.extract_frame` 의 반환이 `bool` → `tuple[int,int] | None`(축소 사본의 크기,
    실패 시 `None`)이고 `reduced=(경로, 긴변)` 인자가 생겼다 — **같은 디코드에서** 원본과
    사본을 함께 쓴다. 저장된 JPEG를 다시 열어 줄이면 손실이 두 번 얹히고 seek·디코드가
    한 번 더 도는데, 둘 다 이 자리에서만 피할 수 있다.
  - **등급(tier)은 두지 않는다.** "먼저 열 이미지"를 코어가 매기면 그것이 변화량 검출과
    같은 자리를 놓고 겨루는 두 번째 추출 기준이 된다 — points.json을 폐기한 사유와 같다.
    무엇을 열지는 읽는 쪽이 정하고, 판단 근거(각 화면이 떠 있던 시간)는 이미 산출물에 있다.
- **`next`가 문자열에서 객체가 됐다.** 전에는 커맨드마다 다른 것이었다 —
  `split`·`transcribe`는 실행 가능한 명령 문자열, `analyze --until`은 맨 스테이지 이름,
  `frames`·`analyze`는 산문. 받는 쪽이 타입을 먼저 알아내야 하니 사슬이 거기서 끊겼다.
  이제 모든 `<video>` 커맨드의 성공 결과가 같은 모양을 낸다:
  `{"do":"run","command":…,"why":…}` · `{"do":"read","read":[…],"command":…,"why":…,
  "remaining":[…],"cost":{"images","image_tokens","rule"}}` · `{"do":"done","why":…}`.
  `cost`가 함께 오는 이유는 "열지 말지"를 정하는 값을 알려고 파일을 하나 더 여는 것이
  순서가 뒤집힌 것이기 때문이다. "읽었는가"를 코어는 알 수 없으므로 `do="review"` 같은
  토큰은 두지 않았다 — 읽기와 제출은 한 걸음이다. 상수 `cli.NEXT_READ_CONTEXT`는 삭제됐다.
- `status` 가 `runs[]` 를 함께 낸다 — 단위별 `context.md` **절대경로**와 `review` 상태.
- `frames.RECOMMENDED_CUT_AREA_THRESHOLD` — 이미지 수를 줄여야 할 때 **먼저 올릴 값**.
  **기본값은 바뀌지 않았다**(`DEFAULT_CUT_AREA_THRESHOLD` 그대로) — 플래그 없이 부른
  실행은 지금과 동일하다. 근거는 영상 6편 × 격자 13점 = 78런 스윕 실측이고, 회수율은
  "기본 임계로 뽑힌 프레임 중 상향 후에도 SSIM>=0.9인 것이 남아 있는 비율"이다:
  컷 면적을 올리면 이미지 **17~43% 감소에 회수율 95.2~100%**, 앵커는 1~15% 감소에 회수율이
  **83%까지 하락**(나쁜 거래), 변화율은 올리면 이미지가 **오히려 늘어난다**(−8~+2% — 그
  값이 "화면이 정착했다" 판정을 겸해 올릴수록 더 이른 시각을 촬영한다). 세 값 결합
  프리셋은 컷 단독보다 회수율이 낮으면서 감소폭은 비슷해 파레토 열세였다. 대사는 임계를
  올려도 잃지 않는다 — 화면 74→34에서도 62문장이 정확히 한 번씩 실렸다
  (`align.assign_segments`의 최대 겹침 배정이 화면 수와 무관하게 완전 분할을 보장한다).
- `analysis-video install-skill` — AI 에이전트용 사용법을 전역에 심는다.
  Claude Code는 `~/.claude/skills/`, 그 밖의 하네스는 `--agents-file` 로 규칙 파일에
  멱등 삽입(마커 구간 교체).
- `doctor` 가 STT 모델 가중치의 저장소·캐시 상태(`repo`/`cached`/`cache_dir`)를
  네트워크 없이 보고한다. 첫 전사 전 워밍업 겸 사전점검으로 쓸 수 있다.
- 종료코드 4의 새 사유 `stt-model-unavailable` — 모델 가중치를 가져올 수 없는 환경.
- 종료코드 2의 새 사유 `schema-mismatch` — 이 버전이 읽을 수 없는 형식의 분석 디렉터리
  (위 "파괴적 변경" 참조). `state.json`·`metadata.json` 어느 쪽을 읽다 걸리든 같은 사유다.

### 변경
- **`doctor` 는 능력 목록을 보고할 뿐 실패하지 않는다 — STT 백엔드가 없어도 종료코드 0.**
  전에는 "백엔드 없음 = 환경 고장"이라 단정해 종료코드 4를 냈다. 그래서 자막이 딸린
  영상을 끝까지 분석할 수 있는 기계가 빨간불을 받았고, 스킬 문서에는 "`doctor` 가
  안 된다고 해도 분석은 된다"는 해명을 사람이 손으로 덧붙여야 했다. 해명이 필요한
  진단은 진단이 아니다. 출력이 `modules.{required,optional}` 과
  `capabilities.{speech-recognition,debug-report}` 로 갈렸고, `ok: false` · 종료코드 4는
  **필수** 모듈이 빠졌을 때(`core-deps-missing`)만 난다. 종료코드 4가 없어진 것이
  아니라 **발화 지점이 옮겨 갔다** — 백엔드 결손은 그것이 실제로 필요해지는 전사
  자리에서 난다.
- **`install-skill --agents-file` 이 마커가 성하지 않으면 아무것도 쓰지 않고 종료코드 2로
  거부한다(`agents-file-markers`).** 전에는 `BEGIN.*?END` 정규식으로 블록을 찾았는데,
  `END` 마커만 남은 파일(사용자가 `BEGIN` 줄을 지운 경우 등)에서 그 탐색이 빗나가
  **파일 전체를 생성 블록으로 덮어썼다** — 사용자가 쓴 `AGENTS.md` 본문이 통째로
  사라졌다. 재현해서 확인한 사고다. 이제 마커 개수와 순서를 먼저 판정해
  0개면 덧붙이고, `BEGIN`→`END` 한 쌍이면 그 구간만 오프셋으로 잘라 갈아 끼우며,
  그 밖의 모양(짝 없는 마커·두 쌍·역순)은 개수와 줄 번호를 `details` 에 실어 거부한다.
  쓰기는 `manifest.write_text_atomic` 을 거친다 — 도중에 죽어도 원본이 반쯤 남지 않는다.
- **`SKILL.md` 에서 숫자를 모두 뺐다.** 임계 기본값·권장값을 스킬 문서에 박아 두면
  그 파일은 설치한 날의 스냅숏이 되어 검출기의 실제 상수와 갈라진다. 이제 "그 값은
  `agent-guide` 에서 받아라"라고만 적고, 값 자체는 상수에서 조립되는 `agent-guide` 가
  낸다. 정성적 함정 경고(`--rate-threshold` 를 올리면 이미지가 오히려 는다)는 남겼다 —
  그것은 숫자가 아니라 방향이다.
- **전사 입력이 된 자막 파일이 바뀌면 `--force` 없이도 다시 전사한다.** "완료된 스테이지는
  그대로 재사용한다"의 예외다 — 입력이 달라졌으면 결과도 달라져야 한다. 영상이 바뀐 경우
  (분석 디렉터리 자체가 다른 분석이 되므로 오류로 중단)와는 취급이 다르다.
- 자막으로 전사한 경우 `--model` 은 쓰이지 않으며 `outputs.model_size` 가 `null` 로 기록된다.
  요청한 모델명을 그대로 적으면 "자막 전사를 `small` 로 했다"는 거짓이 되고, 재사용 안내가
  그 거짓을 근거로 헛된 재전사를 권하게 된다.
- **출력 경로가 모두 절대경로가 되었다.** `<video>` 를 상대경로로 주거나 실행 폴더를
  바꿔도 재개된다. 이전에는 1회차 실행의 작업 폴더가 `state.json` 에 숨은 기준점으로
  박혀, 다른 폴더에서 이어 돌리면 해당 분석 디렉터리를 영구히 못 쓰게 됐다.
- `next` 와 선행 스테이지 안내(`hint`)가 그대로 실행 가능한 명령이 되었다 —
  원본 경로와 `--out` 을 포함한다.
- Intel Mac에 STT가 생겼다(faster-whisper). `onnxruntime` 이 macOS x86_64 설치 파일을
  다시 제공해 설치 파일만으로 해석된다. 해석은 검증, 전사 실행은 미검증.
- README를 현행 계약으로 다시 썼다. 이전 README는 이미 제거된
  `frames --points points.json` 워크플로를 설명하고 있었다. 이번에는 "자막만으로는 부족"
  이라는 서술을 "자막**도** 쓰되 화면을 더한다"로 다듬고, 전사 사다리·자막 거부 기준·
  `yt-dlp` 입력 규격 절을 더했다.
- (내부 API) `split.split_media()` 반환이 2원소 `(audio, video)` 에서 3원소
  `SplitResult` 로 늘었다. 위치 언패킹으로 받던 코드는 즉시 `ValueError` 로 멈춘다 —
  dict를 돌려주어 자막이 조용히 버려지는 것보다 낫다고 봤다. (첫 칸은 그 뒤 다시
  `has_audio: bool` 로 바뀌어 최종 모양은 `SplitResult(has_audio, video, subtitles)`
  다 — 위 "파괴적 변경" 참조.)

### 제거

- **`audio.wav` 를 아예 만들지 않는다.** `split` 이 만드는 것은 `video.mkv` 와
  `subs/track{n}.srt` 뿐이고, `split._extract_audio` 는 삭제했다. 위스퍼는 **원본 영상을
  직접 디코드**한다. 근거는 실측이다 — `media.load_audio_mono16k` 가 컨테이너를 가리지
  않아 원본에서 디코드한 것과 뽑아 둔 wav에서 디코드한 것이 **비트 동일**했다(영상 3편,
  최대차 0.0). 즉 `audio.wav` 는 아무도 최종 산출물로 읽지 않는 **순수 중간 산물**이었고,
  그 대가로 분석 디렉터리의 **33%(분당 1.92MB)** 를 차지했다. 분당 5.85MB → 3.93MB.
  드러나는 자리 셋: `SplitResult` 첫 칸(위 "파괴적 변경"), `state.json` 의
  `stages.split.outputs.audio` → `has_audio`, 그리고 `stt.transcribe_audio` 의 첫 인자
  이름이 `audio_path` → `media_path` 로 바뀐 것(이제 받는 것이 오디오 파일이 아니다).

### 수정
- **심볼릭 링크로 가리킨 영상의 출력 경로가 링크의 실체 옆에 잡히던 문제.**
  자막 작업과 무관한 독립 수정이다 — 위 "출력 경로가 모두 절대경로가 되었다"를 구현하는
  과정에서 드러났고, 고친 자리도 경로 함수 하나뿐이다. 절대경로화를 `Path.resolve()` 로 하면
  링크까지 실체 경로로 바뀐다: `~/videos/lecture.mkv -> /mnt/big/L01.mkv` 를 분석하면 출력이
  사용자가 준 경로 옆(`~/videos/lecture.mkv.analysis`)이 아니라 `/mnt/big/L01.mkv.analysis` 에
  생긴다. 그래서 그 자리에 이미 끝나 있던 분석은 어디에서도 보이지 않고(`state.json` 을 못 찾아
  이어하기가 성립하지 않는다) 전사·검출이 처음부터 다시 돈다 — 실측으로 확인한 재현이다.
  cwd 의존만 걷어내고 **링크는 따라가지 않는** `cli.absolute()`(= `os.path.abspath`)를 도입해,
  분석 디렉터리의 **이름**은 사용자가 준 경로를 따르게 했다. 원본 동일성 판정은 그대로
  `manifest.check_source` 의 지문이 맡는다 — 그쪽은 `resolve()` 로 실체를 보므로 링크와 실체가
  같은 파일임을 안다.
- **최종 사용자의 설치 실패.** `mlx-whisper` 가 `numba` 를 버전 조건 없이 의존해,
  버전 계산이 numba를 2021년 버전(0.53.1, cp312 이상 설치 파일 부재)까지 후퇴시켜
  소스 컴파일로 넘어가 실패했다. 이 저장소는 `uv.lock` 이 가려 주고 있었다.
  `numba>=0.63` 하한을 선언해 막았다.
- `macOS + Python 3.14` 가 해석 실패로 설치 자체를 거부하던 문제. 백엔드 조건에
  버전 상한을 두어 설치가 되게 했다. 그 조합에서는 `[stt]` 를 붙여도 상류 설치 파일이
  없어 백엔드가 붙지 않으며, `doctor` 가 `speech-recognition` 을 `available: false` 로
  보고한다(종료코드는 0 — 위 "변경" 참조).
- 모델 다운로드 실패(오프라인·프록시 차단·HF 장애)가 종료코드 1 `internal` 로
  나오던 문제 → 종료코드 4 `stt-model-unavailable`.
- 존재하지 않는 `--points` 플래그를 안내하던 `hint`. 따라 실행하면 종료코드 2였다.
- `agent-guide` 의 "the tool opens no network connection of its own" 허위 기재.
  첫 전사는 HuggingFace에서 가중치를 받는다.

### 배포
- **음성 인식(STT)을 `[stt]` extra 로 내렸다 — 기본 설치 디스크 1,180MB → 308MB,
  내려받기 326MB → 108MB(macOS Apple Silicon · Python 3.13 실측).** 줄어든 것의
  대부분은 이 도구가 한 번도 `import` 하지 않는 `torch`(디스크 476MB)다. `mlx-whisper`
  가 조건 없이 의존해 딸려 왔고, 워크스페이스 안에서는 `override-dependencies` 로
  가려 두었으나 그 선언은 최종 사용자에게 전달되지 않는다 — 즉 여기서 재던 값과
  사용자가 겪는 값이 달랐다. 자막이 딸린 영상은 위스퍼를 부르지 않으므로 대다수의
  설치에 이 872MB는 순수한 낭비였다.
  - 필요할 때 설치한다: `uvx 'analysis-video[stt]@latest' …` / `pip install 'analysis-video[stt]'`.
  - `[cuda]` 는 `analysis-video[stt]` 를 포함하므로 따로 붙일 필요가 없다.
  - 없다고 해서 도구가 못 도는 것이 아니다 — 아래 "변경"의 `doctor` 항목 참조.
- MIT 라이선스 명시(`LICENSE` + 패키지 메타데이터).
- PyPI 메타데이터: `readme` · `license` · `authors` · `classifiers` · `keywords` ·
  `urls`. 이전 빌드는 README 본문과 라이선스가 누락되어 PyPI 페이지가 비었을 것이다.
- 릴리스 절차 문서화([`docs/RELEASING.md`](docs/RELEASING.md)) 및 CI 추가
  (테스트 매트릭스 · 워크스페이스 밖 설치 스모크 · 태그 기반 Trusted Publishing).
- 저장소 루트 `SKILL.md` 는 `skill.py` 템플릿의 **생성물**이고 CI가 대조한다.
  손으로 고치지 않는다.

### 알려진 한계
- **자막 거부 임계 6종은 실측으로 보정한 값이 아니다.** 포맷 사양과 도구 관례에 근거해
  잡았을 뿐이고, 이 저장소의 다른 임계(컷 면적 0.002 등)처럼 실제 영상으로 재어 정한 값이
  아니다. 자막이 딸린 영상 표본이 모이면 커버리지 분포와 롤업 비율을 재서 보정해야 한다.
- **ASS/SSA는 읽지 않는다.** 스타일 정의와 본문이 같은 줄에 섞여 별도 파서가 필요하고
  사이드카로 흔하지도 않다. `.ass` 를 `--transcript` 로 주면 거부된다. 다만 **컨테이너
  내장** ASS 트랙은 `split` 이 SRT로 다시 써 두므로 그대로 쓰인다.
- 비트맵 자막(PGS·DVD·DVB) 거부는 코드 경로만 확인했고 실파일로 돌려 보지 못했다.
  ffmpeg이 텍스트→비트맵 트랜스코드를 거부해 표본을 만들지 못했다.
- 내장 트랙은 사이드카와 **같은 목록**에서 겨루고(1차 키가 언어, 출처는 동점 처리),
  트랙끼리의 순서는 **요청 언어(`--sub-lang`, 없으면 시스템 로케일) 일치 → `default` 비트 →
  스트림 인덱스** 순이다. 언어 불일치는 탈락이 아니라 순위 하락이다.
  특정 트랙을 강제하려면 `subs/track{n}.srt` 를 `--transcript` 로 지목한다.

---

# analysis-video-gui (디버깅 GUI)

## [0.1.1] — 2026-08-14

### 변경

- **경로를 푸는 일을 코어에 위임한다** (`manifest.resolve_target`). GUI는 처음부터 비디오
  파일과 `.analysis` 디렉터리를 모두 받았고 CLI는 비디오만 받았는데, 규약이 두 곳에 따로
  있었기 때문이다. 코어가 그 규약을 갖게 되면서 여기서는 코어의 거부를 사용자 언어의 문장으로
  옮기기만 한다 — 오류 문구를 파싱하지 않고 `kind`와 `details`의 값만 쓰므로, 코어가 문구를
  고쳐도 GUI가 조용히 갈리지 않는다. 눈에 보이는 동작은 그대로다.

- **코어 하한이 `>=0.1.1`로 올랐다.** 위 위임 때문이다 — 0.1.0 코어에는 그 함수가 없어,
  하한을 `0.1`로 두면 낡은 코어와 짝지어진 GUI가 기동 첫 줄에서 죽는다.

## [0.1.0] — 2026-08-14

첫 릴리스다. 코어와 **버전은 독립**이고, 같은 0.1.0 이라는 것은 우연이다.

### 배포
- **PyPI에 올린다.** 이전 방침은 "올리지 않고 `git+` 로 설치"였으나 철회했다.
  코어와 **별개 패키지·독립 버전**으로 간다 — GUI만 고쳐도 코어를 다시 올리지 않고,
  CLI만 쓰는 사람에게 Qt가 딸려가지 않는다. `uvx analysis-video-gui <경로>` 로
  설치 없이 실행된다(실측: 62개 패키지, 캐시 적중 시 6초).
- MIT 라이선스 명시(`LICENSE` + 패키지 메타데이터).

### 추가
- **UI 현지화 3종(`ko`·`en`·`ja`)과 실행 중 언어 전환.** 허브의 언어 콤보로 바꾸면 열려 있는
  창들이 즉시 다시 그려진다. 기본 언어는 시스템 로케일을 따르고 선택은 설정에 남는다.
- **설정 저장(QSettings).** 언어와 창 레이아웃이 다음 실행에 복원된다. 조직·앱 식별자를
  `settings.py` 한 곳에만 두어 같은 앱이 서로 다른 저장소에 쓰는 일을 막았다.
- **입력 소스와 무관한 단축키.** `QKeyEvent.key()` 는 "누른 키"가 아니라 그 키가 만든
  **문자**라, 한글 입력 상태에서 K를 누르면 `Key_K` 가 아니라 자모 'ㅏ'(U+314F)로 온다 —
  문자로 비교하던 단축키가 영문 입력일 때만 동작했다. `keys.py` 가 물리 키 자리를 해석해
  IME 상태와 무관하게 같은 자리가 같은 단축키가 되게 한다.

### 변경
- **의존을 `PySide6` 에서 `PySide6-Essentials` 로 바꿨다. 설치 용량 1.23GB → 333MB,
  다운로드 443MB → 111MB.** `PySide6` 는 껍데기 패키지라 `pyside6-essentials` 와
  `pyside6-addons` 를 함께 끌어오는데, 이 GUI가 import 하는 Qt 모듈은
  `QtCore`·`QtGui`·`QtWidgets` 셋뿐이고 전부 essentials 소유다. addons(설치 후
  884MB)는 한 줄도 쓰지 않으며, 그 안의 QtMultimedia 는 AV1 디코드 실패로 이미
  버린 것이다(`playback.py` 참조). addons 미설치 환경에서 GUI 테스트 전부 통과(현재 121개).

  기존 개발 환경은 제자리 교체 시 addons 제거가 essentials 소유의 공유 파일
  `PySide6/__init__.py` 를 함께 지운다. 한 번만 재설치하면 된다:
  `uv sync --reinstall-package pyside6-essentials`

### 수정
- `[tool.uv.sources]` 선언을 워크스페이스 루트로 옮겼다. `packages/gui` 안에 있으면
  그 폴더를 워크스페이스 밖에서 설치할 때 uv가 워크스페이스를 찾다 실패한다
  (`references a workspace ... but is not a workspace member`). 설치 스모크가 정확히
  그 경로라 CI가 통째로 막혔다. 색인(PyPI)에서 받는 소비자에게는 원래 무해했다 —
  uv는 의존성 쪽 `sources` 를 읽지 않는다.

### 알려진 한계
- **`state.json` 은 코어와 같은 관문을 지나지만, 그 밖의 산출물은 관대하게 읽는다.**
  기동 시 `manifest.load_state` 를 거치므로 형식이 다른 옛 분석 디렉터리는 현지화된 메시지와
  함께 멈춘다(코어의 종료코드 2와 같은 판정). 다만 `runs/index.json`·`metadata.json` 등은
  `store.py` 가 `json.loads` + 예외 무시로 읽는다 — CLI가 쓰는 도중일 수 있어 일부러 관대하게
  둔 것이고, 그래서 그쪽 형식 변화는 GUI가 잡지 못한다.
- 리눅스 오디오 재생에 시스템 패키지 `libportaudio2` 가 필요하다. `sounddevice` 는
  macOS·Windows용 설치 파일에만 PortAudio를 동봉한다. 없어도 설치는 성공하고
  재생 시점에 실패한다.
- 리눅스 aarch64는 glibc 2.31 미만에서 Qt 설치 파일이 없어 설치되지 않는다.
  오래된 glibc에서는 최신 Qt가 아니라 구버전으로 떨어진다(x86_64 glibc 2.28 →
  PySide6 6.9.3, aarch64 glibc 2.31 → 6.8.0.2). Windows ARM64는 코어와 같은 이유로
  미지원.
